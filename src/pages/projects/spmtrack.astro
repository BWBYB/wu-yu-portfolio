---
import { ArrowLeft, ArrowUpRight } from '@lucide/astro';
import { getEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const project = await getEntry('projects', 'spmtrack');
if (!project) throw new Error('SPMTrack content not found');
---

<BaseLayout title="SPMTrack · Wu Yu" description="SPMTrack 视觉目标跟踪系统项目案例。" activePath="/projects/">
	<section class="detail-hero page-shell"><div><p class="eyebrow">GRADUATION PROJECT / 2025.11—2026.06</p><h1>SPMTrack<br /><em style="color: var(--terracotta); font-style: normal;">视觉目标跟踪系统。</em></h1><p class="detail-hero__summary">将视觉跟踪研究代码封装成可演示、可复现、可部署的视频目标跟踪 Web 系统。</p></div><div class="detail-hero__side"><div class="detail-meta"><span>我的角色</span><strong>前端 · 全栈工程</strong></div><div class="detail-meta"><span>核心技术</span><strong>React / FastAPI / Docker</strong></div><div class="detail-meta"><span>项目状态</span><strong>可交互 · 可部署</strong></div><a class="button-primary" href="https://github.com/BWBYB/SPMTrack" target="_blank" rel="noreferrer">查看原始仓库 <ArrowUpRight size={16} aria-hidden="true" /></a></div></section>
	<section class="detail-content page-shell"><nav class="detail-content__nav" aria-label="项目目录"><a href="#context">项目背景</a><a href="#architecture">四层架构</a><a href="#workflow">任务流程</a><a href="#contribution">我的工作</a><a href="#reflection">工程复盘</a></nav><article class="detail-content__body">
		<section id="context"><p class="eyebrow">01 / CONTEXT</p><h2>从研究实现，到可用系统。</h2><p>{project.data.summary} SPMTrack 的上游实现是 CVPR 2025 的视觉目标跟踪研究代码；我围绕它搭建了面向浏览器用户的完整应用流程，把原本依赖命令行和脚本串联的实验过程，整理成可以上传视频、选择目标、创建任务、查看日志和回放结果的 Web 系统。</p><div class="callout"><p>这不是对算法论文的重复介绍，而是一次从研究实现到软件交付的工程实践：让用户能看见任务状态，让系统能被验证，也让项目可以被复现和交接。</p></div></section>
		<section id="architecture"><p class="eyebrow">02 / RUNTIME ARCHITECTURE</p><h2>四层边界，各自负责。</h2><div class="architecture"><div class="architecture__layer"><strong>01 / 前端工作台</strong><span>React + TypeScript 负责视频上传、ROI 选择、任务历史、结果回放、日志和监控页面。</span></div><div class="architecture__layer"><strong>02 / 网关层</strong><span>Spring Boot 提供稳定的浏览器 API，处理校验、上传边界和错误透传。</span></div><div class="architecture__layer"><strong>03 / 推理服务</strong><span>FastAPI 管理 SQLite 任务记录、队列执行、日志、超时和进程取消。</span></div><div class="architecture__layer"><strong>04 / 跟踪引擎</strong><span>复用 PyTorch、OpenCV 和原有评测脚本，生成最终跟踪结果视频。</span></div></div></section>
		<section id="workflow"><p class="eyebrow">03 / TASK WORKFLOW</p><h2>让长任务，也有清晰的进度。</h2><div class="flow"><span class="flow__step">上传视频</span><span class="flow__arrow">→</span><span class="flow__step">选择 ROI / 起始帧</span><span class="flow__arrow">→</span><span class="flow__step">创建任务</span><span class="flow__arrow">→</span><span class="flow__step">队列执行</span><span class="flow__arrow">→</span><span class="flow__step">结果回放</span></div><div class="state-grid"><div class="state"><strong>PENDING</strong><span>等待执行</span></div><div class="state"><strong>RUNNING</strong><span>任务运行中</span></div><div class="state"><strong>SUCCESS</strong><span>结果可用</span></div><div class="state"><strong>FAILED</strong><span>错误可追踪</span></div><div class="state"><strong>CANCELED</strong><span>任务已取消</span></div></div><div class="callout"><p>执行阶段还会展示 <strong>preparing → evaluating → rendering → transcoding</strong>，访客可以看到任务具体卡在哪一步。</p></div></section>
		<section id="contribution"><p class="eyebrow">04 / MY CONTRIBUTION</p><h2>我负责的是“交付层”。</h2><ul><li>基于 React + TypeScript 搭建任务工作台，使用 Canvas 实现浏览器内 ROI 框选，并完成视频帧预览与源帧坐标转换。</li><li>参与 Spring Boot 网关开发，统一封装任务创建、查询、取消、重试、日志、结果视频和资产接口。</li><li>基于 FastAPI 封装推理任务流程，把原有脚本纳入队列和状态机，并处理 SQLite 持久化、日志、超时和取消。</li><li>使用 Recharts 展示任务统计、运行状态和失败类型，使用 Docker 组织前端、网关和推理服务。</li><li>配合健康检查和 smoke test 验证上传、创建任务、查看结果和取消任务等主链路。</li></ul></section>
		<section id="reflection"><p class="eyebrow">05 / REFLECTION</p><h2>边界补齐之后，算法才真正能被使用。</h2><p>这个项目最重要的变化不是把算法“放进网页”，而是把研究代码周围的边界补齐：浏览器只面对稳定的任务契约，网关负责输入和访问边界，推理服务负责长任务生命周期，底层脚本继续专注于跟踪和渲染。这样的拆分让系统更容易复现，也让调试和演示有了清晰的入口。</p><a class="text-link" href="/projects/">返回项目列表 <ArrowLeft size={15} aria-hidden="true" /></a></section>
	</article></section>
</BaseLayout>
